# 环境搭建与整体思路

## 1. 环境搭建（基于你当前的实际配置）

### 1.1 基础环境
- 操作系统：Windows
- Python/Conda：`rag_test` 环境
- 运行方式：PowerShell + Python 脚本

### 1.2 依赖安装
已整理在 `requirements.txt`，核心依赖包括：
- `langchain` / `langchain-classic` / `langchain-core`
- `langchain-community`
- `langchain-huggingface`（嵌入模型接口）
- `langchain-chroma`（向量数据库接口）
- `langchain-ollama`（本地 LLM 接入）
- `chromadb`（向量数据库）
- `sentence-transformers`
- `modelscope`（国内镜像下载）

### 1.3 文档目录
- 文档放置位置：`./documents/`
- 支持格式：`txt` / `pdf` / `md`

### 1.4 嵌入模型（Embedding）
使用 ModelScope 国内镜像下载：
- `BAAI/bge-small-zh-v1.5`
- `BAAI/bge-base-zh-v1.5`

本地缓存目录：`./models/`  
已优化：**先检测本地缓存再下载**，避免重复下载。

### 1.5 向量数据库
使用 ChromaDB 存储与检索向量：
- 数据库存放：`./data/`
- 每个嵌入模型使用独立数据库目录，避免维度冲突  
  例如：`./data/chroma_db_bge-base-zh-v1.5`
已优化：**检测已有数据库并复用**，不重复构建。

### 1.6 本地 LLM（Ollama）
已配置为 D 盘：
- 程序路径：`D:\Ollama\`
- 模型路径：`D:\Ollama\models\`
- 常用模型：`qwen2.5:7b` / `glm4:9b`

验证命令：
```powershell
ollama --version
ollama list
```

---

## 2. 整体思路（RAG 的完整流程）

RAG = Retrieval + Augmented + Generation  
核心是：**检索外部文档 → 提供上下文 → LLM 生成答案**

### 2.1 离线阶段：构建知识库
1. **文档加载**：从 `documents/` 读取文本
2. **文档分块**：`chunk_size=500` / `chunk_overlap=100`
3. **文本向量化**：用 BGE 模型生成向量
4. **向量存储**：写入 ChromaDB（持久化）

产物：
- 向量数据库文件（可复用）
- 文档块与向量的映射

### 2.2 在线阶段：回答问题
1. **用户问题** → 嵌入模型向量化
2. **向量检索** → 找到最相似的文档块
3. **LLM 生成** → 文档 + 问题 → 本地 Ollama 生成答案
4. **返回结果** → 显示答案与来源

---

## 3. 当前项目的执行入口

主脚本：`rag_with_llm_ollama.py`

运行方式：
```powershell
conda activate rag_test
python rag_with_llm_ollama.py
```

---

## 4. 已实现的关键优化

### ✅ 模型不重复下载
优先使用本地缓存：
```python
snapshot_download(..., local_files_only=True)
```

### ✅ 向量库复用
检测已有数据库文件：
- `chroma.sqlite3`
- `*.parquet`

存在时直接加载，不重复构建。

---

## 5. 系统能力总结

- ✅ 本地文档检索
- ✅ 中文语义检索（BGE 系列）
- ✅ 本地 LLM 生成（Ollama）
- ✅ 模型与向量库缓存复用
- ✅ 可扩展与可持续维护

---

## 6. 可扩展方向（可选）

- 多轮对话记忆
- 引用片段展开/高亮
- 增加 Rerank 提升检索准确度
- Web UI（Streamlit/Gradio）

---

**总结**：  
当前系统已经是完整的本地 RAG 实验链路，具备“文档检索 + 语义理解 + 本地生成”的闭环能力，可直接用于后续实验或扩展。  
